
console.log('Server using simple memory storage - client uses SharedStorageManager for persistence');

export const isDatabaseAvailable = false;
export const pool = undefined;
export const db = undefined;